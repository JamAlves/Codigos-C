#include<stdio.h>
#include<stdlib.h>
//#include<math.h>

main(void)
 {    // livre da maldição 
     printf("hello world");
     
     /*pausa o programa após executar-lo*/
     system("\npause");
     
     // identificadore válidos 
        A
        a 
        media
        altura2
        media_idade
        x36
        // identificadore não válidos
         2@ 
         b#
         media idade
         x*y
         #media
         idade
         // Tipo de dadaos basicos em C
          char, int , float double, enum, void, pointer
          
          char: composto por caracter simples e strings
          int: são dados numericos que não possuem casas decimais
          float:valores com ponto flutuante, numeros decimais
          double: valores com ponto flutuante de precisão dupla
          enum:dados enumerados, possibilitam os tipos definidos pelo usuario.
          void: significa valores que ocupam 0 bits e não tem valor
          pointer: representa um tipo especial que tem informação e sim localização
          
          unsigned: e utilisado para declarar os tipos como sem sinal , duplicando assim a 
          gama de valores 
          
          unsigned char, 
          unsigned int.
          unsigned long
          
          short: reduz a capacidade de armazenamento , enquanto long aumenta.
          short int 
          short long 
          
          // Variaveis mais usadas 
          int quantidade;
          float total;
          float valor, total;
          char sexo;
          char endereço[30];
          // constante e dado por um valor fixo 
          #define  int = 30
          
          
          
     

 return(0);
}
